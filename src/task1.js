const arr = []

// Escribe tu codigo por debajo de esta linea
//*Para llenar el arreglo con números del 1 al 20, en orden, utilizando el método .push, se hará uso de un ciclo for.
for(let i=1; i<=20; i++){
  arr.push(i);
}
console.log(arr);
//* Recordemos que el método push anexa elementos al final del array, entonces para que salga de manera ordenada, es necesario anexar en orden ascendente, para lo cual el ciclo for es muy útil.

// Escribe tu codigo por encima de esta linea

//No tocar esta funcion
export function taskArr(){
  return arr;
}
